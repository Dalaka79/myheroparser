# myheroparser

🖥️ VLab — Парсер публичных VPN конфигов

VLab — это инструмент для сбора публичных VPN-конфигураций из репозитория, фильтрации дубликатов, базовой проверки доступности и генерации готовых подписок для клиентов.

⚠ Важно: VLab **не является VPN-сервисом**. Он **только собирает публичные конфиги**, не настраивает серверы и не гарантирует их работу. Использование конфигов — на ваш страх и риск.


## ⚙️ Как работает VLab

1. Получает **все конфиги** из репозитория:
   - `BLACK_VLESS_RUS.txt` → VLESS  
   - `BLACK_SS+All_RUS.txt` → SS  
2. Убирает дубликаты  
3. Проверяет базовую доступность host:port (TCP)  
4. Переименовывает конфиги в читаемый вид, например:
5. 5. Формирует три версии конфигов в папке `configs/`:
   - `config.txt` → публичный, максимум **5 VLESS + 5 SS**  
   - `config_base64.txt` → Base64 версия для клиентов  
   - `config_owner.txt` → приватный, полный список всех ссылок  

---

## 📌 Поддерживаемые клиенты

- **FlClash** — Android / ПК (рекомендуется, поддерживает автообновление Base64)  
- **Happ** — iOS / Android  
- **Karing** — iOS / Android  
- **v2rayNG** — Android  
- **Throne** — Linux  

> Рекомендуем сначала использовать VLESS, так как он более стабильный и современный. В `config.txt` сначала идут VLESS-конфиги.


